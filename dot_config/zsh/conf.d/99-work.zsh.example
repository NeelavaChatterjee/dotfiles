# Copy to 99-work.zsh and fill in.
# Gitignored — corp/work-internal config lives here (never committed).
# Examples:
# export AWS_PROFILE=...
# alias ksaml='saml2aws login -a ...'
# alias kdev='kubectl --context=...'
